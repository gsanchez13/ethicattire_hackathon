import React from "react";
import Header from "./Header";

const GetItems = (props) => {
    {console.log(props)}
    return(
        <div>
            {

            }
        </div>
    );
}

export default GetItems;